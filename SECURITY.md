# Security Policy

Do not report suspected vulnerabilities in a public issue. Use GitHub private
vulnerability reporting after the repository is published.

This repository is an educational sample, not a managed service. Before production
use, replace temporary administrative deployment access with least-privilege roles,
use a complete JSON Schema validator, add alarms, review retention requirements, and
perform a threat-model review.
